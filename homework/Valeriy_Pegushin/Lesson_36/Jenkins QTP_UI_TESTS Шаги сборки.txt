cd test_valpeg_testshop_ui
git pull

if [ ! -d "venv" ] || [ ! -f "venv/bin/activate" ]; then
    rm -rf venv
    python3 -m venv venv
fi

. venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
pytest -v
